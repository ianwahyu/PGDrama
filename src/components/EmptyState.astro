---
const { title = "Belum ada data", message = "Coba kata kunci atau kategori lain." } = Astro.props;
---

<div class="soft-panel p-8 text-center">
  <p class="text-xl font-black">{title}</p>
  <p class="mt-2 text-sm font-bold text-content-100">{message}</p>
</div>
